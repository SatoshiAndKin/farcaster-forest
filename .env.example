# Farcaster Forest - Environment Variables Template
# Copy this file to .env and fill in your values

# Development Configuration
# Set to "development" or "production"
# RUST_LOG=info

# Web Build Configuration
# Port for trunk serve (default: 8080)
# TRUNK_SERVE_PORT=8080

# Mobile Build Configuration
# Android SDK path (for cargo-apk)
# ANDROID_HOME=/path/to/android/sdk
# ANDROID_NDK_ROOT=/path/to/android/ndk

# Build Optimization
# Set to "1" to enable release optimizations in dev builds
# CARGO_PROFILE_DEV_OPT_LEVEL=1

# Audio Configuration
# Spatial audio settings (optional)
# MAX_BIRDS=8
# BIRD_SPAWN_INTERVAL=3.0

# Platform-Specific
# macOS deployment target (default: 11.0)
# MACOSX_DEPLOYMENT_TARGET=11.0

# CI/CD (for GitHub Actions - do not set locally)
# GITHUB_TOKEN=<secret>
# CARGO_TERM_COLOR=always

# Notes:
# - This is a game application with no backend dependencies
# - Most configuration is handled through Cargo.toml and Trunk.toml
# - These variables are optional for local development
# - Mobile builds require platform-specific SDKs (see AGENTS.md)
